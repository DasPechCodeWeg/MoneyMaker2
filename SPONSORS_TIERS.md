# Owner setup: the one step that turns this into a paid product

Enable GitHub Sponsors for the `DasPechCodeWeg` account: go to https://github.com/sponsors/accounts, pick the personal account, and complete the Stripe payout form. The form is private. Then create these two one-time tiers:

| Tier | Type | Amount | Tier description (paste) |
| --- | --- | --- | --- |
| Upgrade audit | One-time | $29 | Payment for a delivered py315ready upgrade audit. Pay only if you were satisfied. |
| Unblock PR | One-time | $149 | Payment for a delivered Python 3.15 unblock PR. Pay only if you were satisfied. |

Keep the public Sponsors profile under the GitHub handle only. Do not add a legal name, because the real name must not appear publicly.
